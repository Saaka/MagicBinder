import React from "react";
import {useDocumentTitle} from "Hooks";

const CardPage = (props) => {
    useDocumentTitle("Set card name later");

    return (
        <div>
        </div>
    );
}

export {CardPage};